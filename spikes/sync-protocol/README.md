# Sync protocol spike

This executable proof covers durable operations, checkpointed push/pull, duplicate delivery, browser restart, a single shared multi-tab lock, and device-scoped idempotency.

```bash
pnpm install
pnpm check
```
